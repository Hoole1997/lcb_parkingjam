import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.carout.parkingjam',
  appName: '车水马龙',
  webDir: 'dist',
  android: {
    backgroundColor: '#1a2332',
  },
}

export default config
